# Generated by AppFactoryKit appgen. Run: xcodegen generate
name: PhotoEnhancerUnblur
options:
  bundleIdPrefix: com.zubeid
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

packages:
  AppFactoryKit:
    url: https://github.com/ZubeidHendricks/AppFactoryKit
    from: "0.1.0"

targets:
  PhotoEnhancerUnblur:
    type: application
    platform: iOS
    sources:
      - Sources
    dependencies:
      - package: AppFactoryKit
        product: AppFactoryKit
      - package: AppFactoryKit
        product: AppFactoryKitRevenueCat
    info:
      path: Sources/Info.plist
      properties:
        CFBundleDisplayName: "PhotoEnhancerUnblur"
        UILaunchScreen: {}
        UISupportedInterfaceOrientations: [UIInterfaceOrientationPortrait]
        NSCameraUsageDescription: "PhotoEnhancerUnblur uses the camera to capture and process your content."
        NSPhotoLibraryUsageDescription: "PhotoEnhancerUnblur needs access to your photos to import and process images."
        NSPhotoLibraryAddUsageDescription: "PhotoEnhancerUnblur saves your results to your photo library."
    settings:
      base:
        PRODUCT_NAME: PhotoEnhancerUnblur
        PRODUCT_BUNDLE_IDENTIFIER: com.zubeid.photoenhancer
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        TARGETED_DEVICE_FAMILY: "1"
        SWIFT_VERSION: "5.0"
        GENERATE_INFOPLIST_FILE: NO
        CODE_SIGN_STYLE: Automatic

  PhotoEnhancerUnblurTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - Tests
      - path: Sources/EnhanceService.swift
    settings:
      base:
        PRODUCT_NAME: PhotoEnhancerUnblurTests
        PRODUCT_BUNDLE_IDENTIFIER: com.zubeid.photoenhancerunblur.tests
        GENERATE_INFOPLIST_FILE: YES
        TARGETED_DEVICE_FAMILY: "1"
        SWIFT_VERSION: "5.0"

schemes:
  Tests:
    build:
      targets:
        PhotoEnhancerUnblurTests: [test]
    test:
      targets:
        - PhotoEnhancerUnblurTests
